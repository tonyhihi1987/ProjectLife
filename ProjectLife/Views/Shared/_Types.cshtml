@model List<TypeFilterViewModel>
<form method="post" action="Project/ApplyFilter" >
   @for (int j = 0; j < Model.Count; j++)
   {

    <div>
        <input asp-for="@Model[j].IsChecked" type="checkbox" />
        <label>@Model[j].Name</label>
        <input asp-for="@Model[j].Name" type="hidden" />
    </div>


   }
    <button class="btn btn-primary fa fa-check" type="submit" style="float:right"></button>
</form>